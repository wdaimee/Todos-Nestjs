import styled from 'styled-components';
import { StyledButton } from '../Button/Button.styles';

export const StyledAddTodoButton = styled(StyledButton)`
    margin-top: 10px;
    display: inline-block;
    width: 6rem;
`;